# Environment configuration template for OptiCrop
# Copy this file to .env and fill in real values before running in production

FLASK_ENV=development
PORT=5000

# Not required for the current version (no external DB/API calls),
# included for future extension (e.g. if a database or external API is added)
# DATABASE_URL=
# SECRET_KEY=
